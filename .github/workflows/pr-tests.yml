name: "PR: Simulations & Tests"

on:
  pull_request:
    types: [opened, reopened, synchronize]
    paths:
      - "simulations/**"
      - "tests/**"

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: |
          set -euo pipefail
          npm ci

      - name: Install Playwright browsers
        run: |
          set -euo pipefail
          npx playwright install

      - name: Run unit tests
        run: |
          set -euo pipefail
          npm test

      - name: Run E2E tests
        run: |
          set -euo pipefail
          npm run test:e2e
