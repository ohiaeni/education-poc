function setup() {
  createCanvas(400, 300);
  background(220);
  textSize(24);
  textAlign(CENTER, CENTER);
  fill(50);
  text('p5.js が正常に読み込まれました', width / 2, height / 2);
}

function draw() {
  // 静的な表示のため draw は空
}
